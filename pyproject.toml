[project]
name = "seisakutools"
version = "0.1.0"
requires-python = ">=3.14"
readme = "README.md"
dependencies = [
    "numpy",
    "matplotlib",
    "scipy",
    "sympy",
    "rdkit",
]

[tool.uv]
package = false

